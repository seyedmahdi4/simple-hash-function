## Use it in php too.

- **How to use**

include **php** file in your project and call the function

- **Example**

```PHP
// Include Lib
include("hashf.php");

// Define a simple text
$text = "PHP-Test";

// Hash in into a variable
$hashed = hashf($text);
```